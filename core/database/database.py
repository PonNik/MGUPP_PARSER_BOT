import pymysql

# the database will be deployed on localhost
class Lessons():
    __tablename__ = 'lessons'
    def __init__(self, uid, days):
        for day in days:
            day['name']
            day['data-start']
            day['data-end']
            day['_auditoria']
            day['data-type']